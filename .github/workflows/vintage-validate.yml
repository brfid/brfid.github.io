name: Validate vintage pipeline

# Runs the pinned pipeline without publishing. Verifies direct artifacts,
# source equivalence, and an optional rendered-bio SHA-256 baseline.

on:
  workflow_dispatch:
    inputs:
      expected_sha256:
        description: "Expected brad.bio.txt SHA256 (blank to record the hash without comparing)"
        required: false
        default: ""

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    # Bound a stalled SIMH console session.
    timeout-minutes: 180

    steps:
      # This job does not build Hugo or require its submodule.
      - name: Checkout
        uses: actions/checkout@v7

      - name: Record host facts
        run: |
          echo "arch:   $(uname -m)"
          echo "kernel: $(uname -sr)"
          echo "docker: $(docker --version)"
          echo "python: $(python3 --version)"
          echo "cpus:   $(nproc)"
          echo "memory: $(free -h | awk '/^Mem:/{print $2}')"
          echo "disk:   $(df -h / | awk 'NR==2{print $4" free"}')"

      - name: Run vintage pipeline
        id: vintage
        env:
          GIT_SHA: ${{ github.sha }}
          ALLOW_LOCAL_IMAGE_BUILD: "0"
          LOG_DIR: /tmp/edcloud-vintage
        run: |
          set -euo pipefail

          BUILD_ID="validate-$(date -u '+%Y%m%d-%H%M%S')"
          echo "build_id=${BUILD_ID}" >> "$GITHUB_OUTPUT"
          echo "log_file=${LOG_DIR}/${BUILD_ID}.log" >> "$GITHUB_OUTPUT"

          start="$(date -u +%s)"
          rc=0
          bash scripts/vintage-runner.sh "$BUILD_ID" || rc=$?
          elapsed=$(( $(date -u +%s) - start ))

          printf 'Pipeline wall clock: %dm%02ds (exit %d)\n' \
            "$((elapsed / 60))" "$((elapsed % 60))" "$rc"
          echo "elapsed_seconds=${elapsed}" >> "$GITHUB_OUTPUT"

          if (( rc != 0 )); then
            echo "::error::Vintage pipeline failed after ${elapsed}s"
            exit "$rc"
          fi

      - name: Collect artifacts and check bio
        env:
          EXPECTED: ${{ inputs.expected_sha256 }}
        run: |
          set -euo pipefail
          mkdir -p out
          sudo chown -R "$(id -u):$(id -g)" build/vintage
          for artifact in brad.bio.txt build.log.html pipeline-status.json; do
            if [[ ! -s "build/vintage/${artifact}" ]]; then
              echo "::error::Final vintage artifact is missing or empty: build/vintage/${artifact}"
              exit 1
            fi
            cp "build/vintage/${artifact}" "out/${artifact}"
          done
          .venv/bin/python -m json.tool out/pipeline-status.json >/dev/null

          .venv/bin/python -m resume_generator.vintage_contract \
            out/brad.bio.txt \
            site.yaml \
            resume.yaml

          # The rendered bio contains no date, so compare its raw digest.
          sha="$(sha256sum out/brad.bio.txt | cut -d' ' -f1)"
          echo "brad.bio.txt: $(wc -l < out/brad.bio.txt) lines, $(wc -c < out/brad.bio.txt) bytes"
          echo "sha256: ${sha}"

          {
            echo "## Vintage pipeline validation"
            echo
            echo "| | |"
            echo "|---|---|"
            echo "| Wall clock | ${{ steps.vintage.outputs.elapsed_seconds }}s |"
            echo "| brad.bio.txt | $(wc -c < out/brad.bio.txt) bytes, $(wc -l < out/brad.bio.txt) lines |"
            echo "| Round trip | ✅ site.yaml identity + resume.yaml basics.summary |"
            echo "| SHA256 | \`${sha}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

          if [[ -z "$EXPECTED" ]]; then
            echo "| Baseline | not compared (no hash given) |" >> "$GITHUB_STEP_SUMMARY"
            echo "::notice::Recorded SHA256 ${sha} (no comparison requested)"
            exit 0
          fi

          if [[ "$sha" == "$EXPECTED" ]]; then
            echo "| Baseline | ✅ identical |" >> "$GITHUB_STEP_SUMMARY"
            echo "::notice::brad.bio.txt matches the baseline"
          else
            echo "| Baseline | ❌ MISMATCH (expected \`${EXPECTED}\`) |" >> "$GITHUB_STEP_SUMMARY"
            echo "::error::brad.bio.txt does not match the baseline"
            echo "  expected: ${EXPECTED}"
            echo "  actual:   ${sha}"
            exit 1
          fi

      # Collect the full log even when the runner fails.
      - name: Collect diagnostics
        if: always()
        continue-on-error: true
        run: |
          set -uo pipefail
          mkdir -p out
          cp "${{ steps.vintage.outputs.log_file }}" out/pipeline.log 2>/dev/null || true
          cp build/vintage/sections.jsonl out/sections.jsonl 2>/dev/null || true
          for artifact in brad.bio.txt build.log.html pipeline-status.json; do
            test -e "out/${artifact}" || cp "build/vintage/${artifact}" "out/${artifact}" 2>/dev/null || true
          done
          ls -la out/

      # Preserve the inputs needed to diagnose a mismatch or failed stage.
      - name: Upload artifacts and log
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: vintage-${{ steps.vintage.outputs.build_id || github.run_id }}
          path: out/
          retention-days: 14
          if-no-files-found: warn
